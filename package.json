{
  "name": "type-safe-api",
  "version": "1.0.0",
  "description": "Type-safe API framework with automatic type inference from Zod schemas",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/examples/api.js",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": ["typescript", "api", "type-safe", "zod", "openapi", "rest"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^22.5.0"
  }
}
